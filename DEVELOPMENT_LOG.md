# 开发日志

## 2026-09-03：第一阶段 RPA 流程搭建

### 项目目标

使用 Python 和 Playwright 自动操作淘宝星河（`https://adstar.alimama.com/`），第一阶段只负责登录、页面导航、项目定位和详情页跳转。数据解析、清洗和写入 Excel 将在第二阶段实现。

### 已完成内容

1. 创建配置驱动的 Python RPA 工程骨架。
2. 接入 Playwright Chromium，支持打开真实浏览器。
3. 实现人工登录确认流程，兼容扫码、验证码和账号选择。
4. 使用 `runtime/auth_state.json` 保存登录状态；该文件已被 Git 忽略，不会上传。
5. 实现登录后按角色和名称精确点击“我的星河”。
6. 实现按项目名称精确定位项目卡片。
7. 通过实际 DOM 检查确认详情入口不是普通链接，而是以下结构中的 `<span>`：

   ```css
   span[data-spm-click*='projectList_detail']
   ```

8. 实现“定位项目名称 → 获取所属 `mux-card` → 悬停卡片 → 点击详情图标 → 返回列表”的循环流程。
9. 增加运行日志、任务结果清单和失败截图目录。
10. 增加调试暂停开关，方便逐页人工确认。

### 已完成的实际测试

以下项目均已成功定位、进入详情页并返回项目列表：

- `Y26-Gotyasatch-大字报Q3`
- `Y26-Gotyasatch-UGCQ3`
- `Y26戈撒驰-UGC-Q3`
- `Y26-Gotyasatch-种草-Q3`
- `戈撒驰7月`
- `Y26-Gotyasatch-种草`

最终测试进程退出码为 `0`。

### 重要技术决策

- 不使用屏幕坐标或图片识别定位详情按钮。
- 不依赖可能动态生成的样式类名，例如 `jMrQTm`。
- 使用项目名称作为卡片锚点，并使用具有业务语义的 `data-spm-click` 属性定位详情入口。
- 当前保持 `pause_each: true`，每进入一个详情页都等待人工确认；正式无人值守运行时应改为 `false`。
- 当前仅收集原始报表，不修改下载文件。

### 当前工程状态

- Python 源码编译检查通过。
- Playwright、PyYAML 和 Chromium 已安装并完成启动测试。
- 淘宝星河登录、“我的星河”导航及六个项目详情跳转均已通过人工验收。

### 下一阶段计划

1. 确认每个项目详情页中需要进入的数据或报表模块。
2. 确认日期、状态等筛选条件。
3. 定位查询和导出按钮。
4. 为每个项目下载原始报表，并规范文件名和目录结构。
5. 校验下载文件是否完整、格式是否正确。
6. 第二阶段再进行数据清洗、合并及 Excel 模板填充。

### 2026-09-04：标准项目数据采集规则

- 标准日期与商家 GMV 流程仅应用于前四个项目。
- `戈撒驰7月` 与 `Y26-Gotyasatch-种草` 保留为特殊项目，暂不执行标准流程。
- 商家 GMV 为人民币金额时去除币种符号和千位分隔符；页面显示 `-` 时记录为 `na`。

### 已知事项

- 淘宝星河可能使登录状态过期，失效后需要重新人工登录。
- 页面结构升级可能导致选择器失效，应优先维护具有业务语义的属性定位。
- 浏览器下载的 Excel 可能带有 Windows 网络来源标记；后续可在下载校验阶段按需解除单文件锁定，不应全局关闭 Office 安全保护。
